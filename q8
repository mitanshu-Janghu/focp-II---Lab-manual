#include <iostream>
using namespace std;
int main() {
    int n;
    cout << "Enter no: ";
    cin >> n;
    for(int i=1; i<=n; i++) {
        if(i % 3 == 0 && i % 5 == 0)
   cout << "BuzzFuzz ";
        else if(i % 3 == 0)
       cout << "Buzz ";
        else if(i % 5 == 0)
         cout << "Fuzz ";
        else
            cout << i << "not div by 3 or 5 ";
    }
    return 0;
}
