#include <iostream>
using namespace std;
int main() {
    int qty;
    float price,total;

    cout<<"enter quantity: ";
    cin>>qty;
       cout<<"price of one item: ";
    cin>>price;
       total=qty*price;
    if(qty>1000)
  total=total*0.9;
    cout<<"total Expense:"<<total;
}
